import 'package:flutter/material.dart';


class MyOrdersPageWidget extends StatelessWidget {
  const MyOrdersPageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
