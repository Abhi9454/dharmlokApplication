import 'package:flutter/material.dart';

class NearByEventsPageWidget extends StatelessWidget {
  const NearByEventsPageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
